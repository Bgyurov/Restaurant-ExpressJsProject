       <div class="meal">
                    <div class="meal-img">
                        <img
                            src="{{image}}">
                    </div>
                    <div class="meal-info">
                        <h1>{{name}}</h1>
                        <p><span>Type: </span>{{type}}</p>
                        <p><span>Weigth: </span>{{weight}}gr</p>
                        <p class="price"><span >Price: </span>${{price}}</p>
                    </div>
                    <a href="/details/{{_id}}" class="btn-details">Details</a>
                </div> 