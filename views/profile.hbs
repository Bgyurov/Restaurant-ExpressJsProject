  <main id="profile">
        <section id="profile-info">
            <div class="profile-image">
                <img src="/images/profileImg.png">
            </div>

            <div class="profile-info">
                <div class="profile-text">
                    <h3 id="platform">Your profile</h3>
                    <p id="price"><span>Username: {{userUsername}}</span></p>
                    <p id="genre"><span>Email: {{userEmail}}</span></p>
                    <p id="genre">Your orders:</p>
                <br>
                    {{#if books}}
                    <ul>

                    {{#each books}}
                    {{>order}}

                    {{/each}}
                    </ul>
                    {{/if}}
                   

                </div>
                    
                <div class="product-btn">
                   {{!-- <div class="author">
                        <a href="/edit/{{._id}}" class="btn-edit">Edit</a>
                        <a href="/delete/{{mealReview._id}}" class="btn-delete">Delete</a>
                    </div> --}}

                    <!-- Logged in user who has already buy this game code-->
                 
                </div>
           
            </div>
        </section>
    </main>
